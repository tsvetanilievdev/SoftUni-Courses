// Write a function that receives a number and prints as result that number multiplied by two

//First solution:
function multiply(number){
    let result = number * 2;
    console.log(result)
}
multiply(5);

//Second solution:
function multiplyNumBy2(input) {
    let number = input;
    console.log(number * 2);
}
multiplyNumBy2(10);